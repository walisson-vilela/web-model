import type { ArgTypes } from '@storybook/react-webpack5'

import type { IntervalCalendarProps } from '../../../../components/Calendar/interfaces'

const argTypes: Partial<ArgTypes<IntervalCalendarProps>> = {
  // color: {
  //   defaultValue: 'blue',
  //   description: 'Specifies the icon color.',
  //   table: {
  //     defaultValue: { summary: '"white"' },
  //   },
  //   control: 'text',
  // },
}

export default argTypes
