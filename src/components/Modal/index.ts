import Component from './component'
import { Audit, ConfirmDelete, ConfirmEdit } from './templates'

const Modal = Object.assign(Component, {
  ConfirmDelete,
  ConfirmEdit,
  Audit,
})

export default Modal
