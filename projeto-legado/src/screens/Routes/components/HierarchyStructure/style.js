import styled from 'styled-components';

export const SelectWrapper = styled.div`
    .inline.field {
        display: flex;
        align-items: center;

        .dropdown {
            flex-grow: 1;
        }
    }
`
