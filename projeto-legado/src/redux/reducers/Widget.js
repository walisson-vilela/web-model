import * as types from '../types/WidgetTypes';


const initialState ={
    isError: false

}


export default function(state=initialState, action){
        switch(action.type){
            case types.WIDGET14_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET14_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET14_FAILURE:{
                return state;
            }

            case types.WIDGET142_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET142_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET142_FAILURE:{
                return state;
            }

            case types.WIDGET15_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET15_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET15_FAILURE:{
                return state;
            }

            case types.WIDGET16_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET16_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET16_FAILURE:{
                return state;
            }

            case types.WIDGET17_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET17_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET17_FAILURE:{
                return state;
            }
            case types.WIDGET18_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET18_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET18_FAILURE:{
                return state;
            }
            case types.WIDGET19_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET19_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET19_FAILURE:{
                return state;
            }

            case types.WIDGET20_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET20_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET20_FAILURE:{
                return state;
            }

            case types.WIDGET21_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET21_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET21_FAILURE:{
                return state;
            }

            case types.WIDGET22_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET22_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET22_FAILURE:{
                return state;
            }

            case types.WIDGET232_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET232_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET232_FAILURE:{
                return state;
            }

            case types.WIDGET23_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET23_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET23_FAILURE:{
                return state;
            }


            case types.WIDGET24_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET24_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET24_FAILURE:{
                return state;
            }
            case types.WIDGET25_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET25_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET25_FAILURE:{
                return state;
            }

            case types.WIDGET26_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET26_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET26_FAILURE:{
                return state;
            }

            case types.WIDGET27_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET27_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET27_FAILURE:{
                return state;
            }

            case types.WIDGET28_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET28_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET28_FAILURE:{
                return state;
            }
            case types.WIDGET29_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET29_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET29_FAILURE:{
                return state;
            }

            case types.WIDGET30_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET30_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET30_FAILURE:{
                return state;
            }

            case types.WIDGET31_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET31_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET31_FAILURE:{
                return state;
            }

            case types.WIDGET32_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET32_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET32_FAILURE:{
                return state;
            }

            case types.WIDGET33_REQUEST:{
                const newState ={ ...state}
                newState.isError  = true;
                return newState;

            }


            case types.WIDGET33_SUCESS:{
                const newState ={ ...state}
                newState.isError  = false;
                return newState;
            }

            case types.WIDGET33_FAILURE:{
                return state;
            }




            default:{
                return state;
            }

        }

}
