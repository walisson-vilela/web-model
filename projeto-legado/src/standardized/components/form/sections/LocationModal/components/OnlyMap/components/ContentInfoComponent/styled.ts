import styled from 'styled-components'

export { TitleParameterRadius as Title } from '../../../WithForm/components/Radius/styled'

export const ContainerContent = styled.div`
  padding: ${({ theme }) => theme.spacings.s3};

  flex: 1;
`
