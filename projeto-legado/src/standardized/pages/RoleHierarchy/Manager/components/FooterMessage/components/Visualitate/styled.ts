import styled from 'styled-components'

export const LinkVisualitate = styled.div`
  font-size: 13px;
  line-height: 16px;

  color: ${({ theme }) => theme.colors.blue};
  text-decoration: underline;
  cursor: pointer;
`
